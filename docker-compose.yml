# Convenience wrapper — same stack as infra/docker/docker-compose.yml
# Usage from repo root:
#   docker compose up -d --build
#   docker compose --profile watchtower up -d

include:
  - path: infra/docker/docker-compose.yml
